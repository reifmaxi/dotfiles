# See wofi(5) for a complete reference.

# Specifies the CSS file to use as the stylesheet.
#style=PATH

# Specifies the CSS file to use as the stylesheet. This option is NOT the same
# as style. Absolute paths are absolute however relative paths are relative to
# the wofi config folder location $XDG_CONFIG_HOME/wofi and NOT the current
# working directory as they are with style. They are also NOT relative to the
# path as specified by --conf. This option comes from rootbar and is probably
# more confusing than it's worth. You should probably use style unless you're
# sure this is what you want.
stylesheet=Base16_Seti_UI.css

# Specifies the colors file to use.
#color=PATH

# Specifies the colors file to use. This option is NOT the same as color.
# Absolute paths are absolute however relative paths are relative to the wofi
# config folder location $XDG_CONFIG_HOME/wofi and NOT the current working
# directory as they are with color. They are also NOT relative to the path as
# specified by --conf. This option comes from rootbar and is probably more
# confusing than it's worth. You should probably use color unless you're sure
# this is what you want.
#colors=PATH

# Specifies the mode to run in. A list of modes can be found in wofi(7).
mode=drun

# Specifies the menu width in pixels or percent of screen size, default is 50%.
# Pixels are used unless the number ends with a %.
width=20%

# Specifies the menu height in pixels or percent of screen size, default is 40%.
# Pixels are used unless the number ends with a %.
#height=40%

# Sets the prompt to be display in the search box, default is the name of the
# mode.
prompt=launch

# Sets the x offset from the location in pixels, default is 0.
#xoffset=0

# Sets the y offset from the location in pixels, default is 0.
#yoffset=0

# If true runs wofi in a normal window instead of using wlr-layer-shell, default
# is false.
#normal_window=false

# If true allows image escape sequences to be processed and rendered, default is
# false.
allow_images=true

# If true allows pango markup to be processed and rendered, default is false.
#allow_markup=true

# Specifies the cache file to load/store cache, default is
# $XDG_CACHE_HOME/wofi-<mode name> where <mode name> is the name of the mode, if
# $XDG_CACHE_HOME is not specified ~/.cache is used.
#cache_file=PATH

# Specifies the term to use when running a program in a terminal. This overrides
# the default terminal run order which is kitty, termite, gnome-terminal,
# weston-terminal in that order.
term=alacritty

# Runs wofi in password mode using the specified character, default is false.
#password=CHARACTER

# If true activiating a search with enter will execute the search not the first
# result, default is false.
exec_search=true

# If true hides the scroll bars, default is false.
#hide_scroll=true

# Specifies the matching mode, it can be either contains or fuzzy, default is
# contains.
#matching=fuzzy

# If true enables case insensitive search, default is false.
#insensitive=true

# If true parses out image escapes and pango preventing them from being used for
# searching, default is false.
#parse_search=true

# Specifies the location. See wofi(7) for more information, default is center.
#location=center

# If true disables multiple actions for modes that support it, default is false.
#no_actions=true

# Specifies the height in number of lines instead of pixels.
#lines=10

# Specifies the number of columns to display, default is 1.
#columns=1

# Specifies the default sort order. There are currently two orders, default and
# alphabetical. See wofi(7) for details.
#sort_order=alphabetical

# If true, instructs wofi to use the dark variant of the current GTK theme
# (if  available). Default is false.
#gtk_dark=true

# Specifies something to search for immediately on opening.
#search=STRING

# Sets the monitor to open on.
#monitor=STRING

# Specifies the orientation, it can be either horizontal or vertical, default is
# vertical.
#orientation=horizontal

# Specifies  the  horizontal  align  for  the  entire scrolled area, it can be
# any of fill, start, end, or center, default is fill.
#halign=fill

# Specifies the horizontal align for the individual entries, it can be any of
# fill, start, end, or center, default is fill.
#content_halign=fill

# Specifies  the vertical align for the entire scrolled area, it can be any of
# fill, start, end, or center, the default is orientation dependent. If vertical
# then it defaults to start, if horizontal it defaults to center.
#valign=center

# Specifies the rate at which search results are updated in milliseconds,
# default is 100.
#filter_rate=100

# Specifies the size of images in pixels when images are enabled, default is 32.
#image_size=32

# Specifies the key to use in order to move up. Default is Up (Up arrow).
# See wofi-keys(7) for the key codes.
#key_up=Up

# Specifies the key to use in order to move down. Default is Down (Down arrow).
# See wofi-keys(7) for the key codes.
#key_down=Down

# Specifies the key to use in order to move left. Default is Left (Left arrow).
# See wofi-keys(7) for the key codes.
#key_left=Left

# Specifies the key to use in order to move right. Default is Right (Right
# arrow). See wofi-keys(7) for the key codes.
#key_right=Right

# Specifies the key to use in order to move forward. Default is Tab.
# See wofi-keys(7) for the key codes.
#key_forward=Tab

# Specifies the key to use in order to move backward. Default is ISO_Left_Tab
# (Shift+Tab). See wofi-keys(7) for the key codes.
#key_backward=ISO_Left_Tab

# Specifies the key to use in order to submit an action. Default is Return.
# See wofi-keys(7) for the key codes.
#key_submit=Return

# Specifies the key to use in order to exit wofi. Default is Escape.
# See wofi-keys(7) for the key codes.
#key_exit=Escape

# Specifies the key to use in order to move one page up. Default is Page_Up.
# See wofi-keys(7) for the key codes.
#key_pgup=Page_Up

# Specifies the key to use in order to move one page down. Default is Page_Down.
# See wofi-keys(7) for the key codes.
#key_pgdn=Page_Down

# Specifies the key to use in order to expand/contract multi-action entires.
# There is no default. See wofi-keys(7) for the key codes.
key_expand=Right

# Specifies the key to use in order to hide/show the search bar. There is no
# default. See wofi-keys(7) for the key codes.
#key_hide_search=KEY

# Specifies the line wrap mode to use. The options are off, word, char, and
# word_char. Default is off.
#line_wrap=off

# Specifies whether x and y offsets should be calculated using the global
# compositor space instead of the current monitor. Default is false.
# This does not play well with locations and using it with them is not advised.
#global_coords=true

# Specifies whether the search bar should be hidden. Default is false.
#hide_search=true

# Specifies whether wofi should be dynamically shrunk to fit the number of
# visible lines or if it should always stay the same size. Default is false.
#dynamic_lines=true

# Specifies the layer to open on. The options are background, bottom, top, and
# overlay. Default is top.
#layer=top
